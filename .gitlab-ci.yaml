image: oven/bun:1

pages:
  cache:
    paths:
      - node_modules/

  script:
    - bun install --frozen-lockfile
    - bun run build

  artifacts:
    paths:
      - public

  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH